---
- name: Start 'immich'
  ansible.builtin.import_tasks: tasks/compose_up.yml
  vars:
    t_app_name: 'immich'
    t_app_data_dirs:
      - '{{ p_dirs.apps_dataa }}/immich/db'
      - '{{ p_dirs.apps_dataa }}/immich/model-cache'
      - '{{ p_dirs.apps_dataa }}/immich/matplotlib'
      - '{{ imch_store_data_path }}/immich/upload'
