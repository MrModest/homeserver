---
- name: Start 'tududi'
  ansible.builtin.import_tasks: tasks/compose_up.yml
  vars:
    t_app_name: 'tududi'
    t_app_data_dirs:
      - '{{ p_dirs.apps_data }}/tududi/db'
      - '{{ p_dirs.apps_data }}/tududi/uploads'
