---
dcr_docker_user: placeholder

dcr_daemon_conf:
  data_root: placeholder

dcr_arch_mapping:  # Map ansible architecture {{ ansible_architecture }} names to Docker's architecture names
  x86_64: amd64
  aarch64: arm64
